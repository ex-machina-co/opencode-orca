name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: jdx/mise-action@v3

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun test

      - name: Verify CLI
        run: |
          VERSION=$(bunx @ex-machina/opencode-orca --version)
          echo "$VERSION"
          echo "$VERSION" | grep -qE '@ex-machina/opencode-orca v[0-9]+\.[0-9]+\.[0-9]+'
