## Summary

<!-- How does the current codebase compare to existing specs for this domain? -->
<!-- If no existing specs found, state that and leave remaining sections empty. -->

## Drifted Specs

<!-- Specs whose requirements no longer match codebase reality. -->

### <!-- Capability Name -->

<!-- Which spec file (openspec/specs/<capability>/spec.md) has drifted?
     What requirements no longer match reality? What's changed? -->

## Obsolete Specs

<!-- Specs that describe behavior that no longer exists or has been replaced. -->

### <!-- Capability Name -->

<!-- Which specs describe behavior that no longer exists or has been replaced?
     Why should they be removed? -->

## Capabilities

### Modified Capabilities

<!-- Use the EXACT existing spec directory name from openspec/specs/.
     A domain has one spec — do not invent sub-capability names.
     Only list if spec-level behavior has drifted from reality.
     Each needs a delta spec file with MODIFIED operations. -->
- `<existing-domain-name>`: <what requirement is drifting and why>

### Removed Capabilities

<!-- Use the EXACT existing spec directory name from openspec/specs/.
     A domain has one spec — do not invent sub-capability names.
     Each needs a delta spec file with REMOVED operations. -->
- `<existing-domain-name>`: <why this spec is obsolete>
