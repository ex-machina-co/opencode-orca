---
description: Formally specify a product domain using the specify-domain schema
---

Run `/opsx-new <domain-name> --schema specify-domain` using the argument provided.

If no argument was provided, ask which domain to specify before proceeding.

Then tell the user they can use `/opsx-continue` to step through artifacts one at a time, or `/opsx-ff` to generate them all at once.
